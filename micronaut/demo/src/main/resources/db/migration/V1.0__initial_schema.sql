CREATE TABLE tasks1 (
                       task_id BIGINT NOT NULL,
                       name VARCHAR(255),
                       description VARCHAR(255),
                       PRIMARY KEY (task_id)
);
